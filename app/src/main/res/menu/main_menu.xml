<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <item
        android:id="@+id/menAdd"
        android:onClick="onMenuItemClick"
        android:title="일기 추가" />

    <item
        android:id="@+id/menSearch"
        android:onClick="onMenuItemClick"
        android:title="일기 검색" />
    <item
        android:id="@+id/menInfo"
        android:onClick="onMenuItemClick"
        android:title="개발자 소개" />

    <item
        android:id="@+id/menFin"
        android:onClick="onMenuItemClick"
        android:title="앱 종료" />
</menu>